---
import PageLayout from '~/layouts/PageLayout.astro';
import { getPermalink } from '~/utils/permalinks';

const metadata = {
  title: 'Kitchen refresh',
  description: 'Case study: kitchen refresh with high-impact upgrades—lighting, hardware, and palette cohesion.',
};
---

<PageLayout metadata={metadata}>
  <article class="mx-auto max-w-4xl px-6 py-14 md:py-20">
    <a class="text-sm underline" href={getPermalink('portfolio')}>Back to portfolio</a>

    <h1 class="mt-4 text-4xl font-semibold md:text-5xl">Kitchen refresh</h1>
    <p class="mt-5 text-lg opacity-90">
      A practical refresh focused on upgrades that improve function and the overall feel—without a full renovation.
    </p>

    <div class="mt-10 rounded-3xl border p-4">
      <img class="aspect-[16/10] w-full rounded-2xl object-cover" src={getPermalink('images/portfolio/kitchen-01.jpg', 'asset')} alt="Kitchen refresh (placeholder)" loading="lazy" />
      <p class="mt-3 text-sm opacity-75">Replace with your best “after” image in natural light.</p>
    </div>

    <section class="mt-12">
      <h2 class="text-2xl font-semibold">What changed</h2>
      <ul class="mt-4 list-disc space-y-2 pl-5 text-sm opacity-90">
        <li><strong>Lighting:</strong> better task lighting and warmer ambient options.</li>
        <li><strong>Hardware:</strong> cohesive finishes for a more intentional look.</li>
        <li><strong>Palette:</strong> reduced competing tones; aligned materials.</li>
        <li><strong>Styling:</strong> simplified surfaces; added a few functional, high-quality objects.</li>
      </ul>
    </section>

    <section class="mt-12">
      <h2 class="text-2xl font-semibold">Gallery</h2>
      <div class="mt-6 grid gap-4 sm:grid-cols-2">
        <img class="aspect-[4/3] w-full rounded-2xl object-cover" src={getPermalink('images/portfolio/kitchen-02.jpg', 'asset')} alt="Gallery image (placeholder)" loading="lazy" />
        <img class="aspect-[4/3] w-full rounded-2xl object-cover" src={getPermalink('images/portfolio/kitchen-03.jpg', 'asset')} alt="Gallery image (placeholder)" loading="lazy" />
        <img class="aspect-[4/3] w-full rounded-2xl object-cover" src={getPermalink('images/portfolio/kitchen-04.jpg', 'asset')} alt="Gallery image (placeholder)" loading="lazy" />
        <img class="aspect-[4/3] w-full rounded-2xl object-cover" src={getPermalink('images/portfolio/kitchen-05.jpg', 'asset')} alt="Gallery image (placeholder)" loading="lazy" />
      </div>
    </section>

    <section class="mt-14 rounded-3xl border p-8">
      <h2 class="text-2xl font-semibold">Prefer high-impact improvements?</h2>
      <p class="mt-3 text-sm opacity-90">
        Thyra can identify the changes that move the needle most—especially in kitchens where lighting and small details matter.
      </p>
      <a class="mt-6 inline-block rounded-2xl bg-black px-6 py-3 font-medium text-white hover:opacity-90" href={getPermalink('services')}>
        Explore services
      </a>
    </section>
  </article>
</PageLayout>
