---
import PageLayout from '~/layouts/PageLayout.astro';
import { getPermalink } from '~/utils/permalinks';

import bgTop from '~/assets/images/portfolio/Vardagsrum-med-salongsdel-och-kamin-6.jpg';
// Temporary: use same image until you add mid/bottom images
import bgMid from '~/assets/images/portfolio/Utomhusmilj-er-7.jpg';
import bgBottom from '~/assets/images/portfolio/K-k-med-matplats.jpg';

const metadata = {
  title: 'Thyra Löwenmark Interior Design',
  description:
    'Interior styling and consultation focused on calm, functional spaces with warmth and cohesion. Based in Sweden, available remotely.',
};

const bgStyle = (img) =>
  `background-image: url(${img.src}); background-size: cover; background-position: center; background-repeat: no-repeat;`;
---

<PageLayout metadata={metadata}>
  <!-- SECTION 1: HERO -->
  <section class="relative" style={bgStyle(bgTop)}>
    <div class="absolute inset-0 bg-black/45"></div>
    <div class="relative mx-auto max-w-6xl px-6 py-14 md:py-20 min-h-[calc(100vh-4rem)] flex items-center">
      <div class="grid gap-10 md:grid-cols-2 md:items-center w-full">
        <div>
          <p class="text-sm uppercase tracking-widest text-white/80">Interior styling & consultation</p>
          <h1 class="mt-3 text-4xl font-semibold leading-tight text-white md:text-5xl">
            Calm, functional interiors—designed around how you live.
          </h1>
          <p class="mt-5 text-lg text-white/90">
            Thyra helps you create a cohesive home through practical guidance on layout, lighting, color, and styling.
            You’ll get clear next steps, curated options, and a plan you can execute with confidence.
          </p>

          <div class="mt-8 flex flex-col gap-3 sm:flex-row">
            <a
              class="rounded-2xl border border-white/60 bg-white/10 px-6 py-3 text-center font-medium text-white hover:bg-white/20"
              href={getPermalink('portfolio')}
            >
              View portfolio
            </a>
            <a
              class="rounded-2xl bg-white px-6 py-3 text-center font-medium text-black hover:opacity-90"
              href={getPermalink('contact')}
            >
              Book a consultation
            </a>
          </div>

          <div class="mt-10 grid gap-4 sm:grid-cols-3">
            <div class="rounded-2xl border border-white/20 bg-white/10 p-4">
              <p class="text-sm uppercase tracking-widest text-white/70">Clarity</p>
              <p class="mt-2 text-sm text-white/85">A prioritized action plan—no overwhelm.</p>
            </div>
            <div class="rounded-2xl border border-white/20 bg-white/10 p-4">
              <p class="text-sm uppercase tracking-widest text-white/70">Cohesion</p>
              <p class="mt-2 text-sm text-white/85">Palette and styling that flows room to room.</p>
            </div>
            <div class="rounded-2xl border border-white/20 bg-white/10 p-4">
              <p class="text-sm uppercase tracking-widest text-white/70">Practical</p>
              <p class="mt-2 text-sm text-white/85">Decisions grounded in budget, function, and timeline.</p>
            </div>
          </div>
        </div>

        <div class="rounded-3xl border border-white/20 bg-white/10 p-4">
          <img
            class="aspect-[4/5] w-full rounded-2xl object-cover"
            src={getPermalink('images/portfolio/hero-01.jpg', 'asset')}
            alt="Interior styling example (placeholder)"
            loading="lazy"
          />
          <p class="mt-3 text-sm text-white/75">
            Optional: remove this card if you want only the background image.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- SECTION 2: PROCESS -->
  <section class="relative" style={bgStyle(bgMid)}>
    <div class="absolute inset-0 bg-black/45"></div>
    <div class="relative mx-auto max-w-6xl px-6 py-14 md:py-20 min-h-[70vh]">
      <h2 class="text-3xl font-semibold text-white">A simple process</h2>
      <p class="mt-3 max-w-3xl text-white/90">
        Thyra’s approach is designed to be efficient: define the direction, prioritize changes, then execute with clarity.
      </p>

      <div class="mt-8 grid gap-6 md:grid-cols-3">
        <div class="rounded-3xl border border-white/20 bg-white/10 p-6">
          <p class="text-sm uppercase tracking-widest text-white/70">1) Discover</p>
          <p class="mt-2 text-sm text-white/90">
            You share photos, goals, constraints, and inspiration. We align on what “better” means for you.
          </p>
        </div>
        <div class="rounded-3xl border border-white/20 bg-white/10 p-6">
          <p class="text-sm uppercase tracking-widest text-white/70">2) Plan</p>
          <p class="mt-2 text-sm text-white/90">
            We define layout priorities, palette direction, and lighting improvements—then outline clear next steps.
          </p>
        </div>
        <div class="rounded-3xl border border-white/20 bg-white/10 p-6">
          <p class="text-sm uppercase tracking-widest text-white/70">3) Style</p>
          <p class="mt-2 text-sm text-white/90">
            You receive styling guidance and curated options, so the space feels cohesive and finished.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- SECTION 3: FEATURED + CTA -->
  <section class="relative" style={bgStyle(bgBottom)}>
    <div class="absolute inset-0 bg-black/45"></div>
    <div class="relative">
      <section class="mx-auto max-w-6xl px-6 py-14 md:py-20">
        <div class="flex items-end justify-between gap-6">
